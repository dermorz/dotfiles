README.md

# Secrets - never manage these
.config/nvim/.env
.config/aerc/accounts.conf

# Editor/scratch leftovers
**/*.bak

# fontconfig runtime state, regenerated per machine
.local/share/fonts/**/.uuid

# Machine-specific git settings (identity, work overrides, tool IDs)
.gitconfig.d/

# Repo tooling, not deployed to $HOME
packages

# Local scratch, not deployed
todo.md
